# 新飞书落盘表核对记录

## 核对对象

- 工作簿：`周报复盘辅助表_简化录入版_v3`
- Token：`AEXLs9PcKhKwzatqtu4cLcKJnFc`
- 核对方式：本机 `lark-cli` 用户身份读取
- 初始修订号：40；补齐字段和新表后修订号：50；2026-08-31 将 `录入_渠道` K 列表头统一为“销售票数”后修订号：210

## 原始结构与页面字段的差异

| 页面/逻辑字段 | 原表情况 | 处理 |
|---|---|---|
| 日业务日期 | `录入_客流`、`录入_票房`只有“日期标签” | 已分别增加“业务日期”列；日期标签仅兼容旧行 |
| 渠道销售票数 | `录入_渠道`原来没有渠道票数字段 | 当前统一使用“销售票数”列；普通渠道取每日“今日票数”，小程序按“今日非线下销售数/今日线下销售数”拆分 |
| 播控 | 原工作簿没有播控表 | 已新增 `录入_播控`，粒度为周期×门店×项目 |
| 次卡累计快照 | 原工作簿没有独立快照表 | 已新增 `录入_次卡累计快照`，粒度为快照日期×门店 |
| 次卡分项 | 没有独立“次卡分项”表 | 现由 `录入_次卡` 的卡种行承接，CARD5/CARD10 分行 |
| 线上销售额、周票房总额 | `录入_周经营`没有直接输入列 | 由 `录入_渠道`、`录入_票房`汇总，落到 `AI周报` 的派生列 |

## 当前 sheet ID

| Sheet | ID |
|---|---|
| 录入_周经营 | `2e0293` |
| 录入_渠道 | `8gIXZX` |
| 录入_客流 | `w4P7CA` |
| 录入_票房 | `shkW25` |
| 录入_次卡 | `AiLcsX` |
| AI周报 | `pMgX2D` |
| 字段说明 | `Z5VZvH` |
| 录入_播控 | `5AbPph` |
| 录入_次卡累计快照 | `8qRgHx` |

## 仍需人工确认的语义

1. 页面字段“平台加售结算额”是否与新表“扣佣结算预估”完全同一口径。
2. 页面字段“日场域客流”是否与新表“ 大盘客流”完全同一口径。
3. 页面目前只有门店合计的“次卡已核销次数”；新表按卡种分行，因此不能把合计随意写入某一卡种，需补 CARD5/CARD10 的核销分项或确认统一承接规则。

这些不确定项已在 `data/field-mapping.json` 标记，不会被同步程序静默猜测。

## 2026-08-31 渠道与次卡口径补充

- `录入_渠道` 的销售票数和销售额均为**全部商品按渠道**的统计，不是“各渠道的次卡销售”。
- `录入_次卡` 只承接独立 `cards` 数据，包括次卡销售、额定权益和核销；不得从 `录入_渠道` 推导。
- 来源表普通渠道按每日 `今日票数` / `今日销售额` 导入；小程序按 `今日非线下销售数` / `今日非线下销售额` 和 `今日线下销售数` / `今日线下销售额` 拆分。
- “开店宝”在业务展示中统一为“大众”；“分销”默认与小程序线下同源，不得和“线下”重复汇总。
